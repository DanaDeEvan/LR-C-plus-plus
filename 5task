#include <iostream>
using namespace std;

int main() {
    int N;
    int count = 0;

    cin >> N;

    while (N > 0) {
        int digit = N % 10;   

        if (digit > 3) {
            count++;
        }

        N = N / 10;           
    }

    cout << count;

    return 0;
}
