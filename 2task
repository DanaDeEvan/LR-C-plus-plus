#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double a, b;
    a = 3;
    b = 0.151;

    double f;
    f = (1 - pow(cos((b / a) * M_PI), 2)) / (0.5 * exp(b) + 0.312 * exp(a));

    cout << "Result f = " << f << endl;
    return 0;
}
